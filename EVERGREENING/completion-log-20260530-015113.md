# EVERGREEN Completion Log — gelap-endhonesa

## Summary

- Repo: `gelap-endhonesa`
- Class: Class A — App Repo
- Branch: `base-evergreen-20260530-014122`
- Runtime target: Node.js 24.x
- Package manager: pnpm 11.5.0 via Corepack
- Live URL: `https://endhonesa.com/`

## Artefact Status

- The app is intentionally frozen as an ENDHONESA artefact.
- The claim deadline copy referencing `17 Agustus 2025` is intentionally preserved.
- Current claim status is considered past/closed by date; Evergreen work did not change the UX copy or claim semantics.

## Baseline Findings

- Repo started on `main` with both `package-lock.json` and `pnpm-lock.yaml`.
- `pnpm-lock.yaml` matched the active dependency set better than `package-lock.json`.
- Original `next lint` script was no longer suitable for modern Next.js.
- Original Next.js version had critical npm audit findings.
- Live URL returned Vercel Challenge `HTTP 429`, recorded as reachable but blocked.
- `NEXT_PUBLIC_TW_CLIENT_ID` was required by the app, but no example env file was committed.

## Monthly / Quarterly Changes

- Created a new Evergreen branch.
- Normalized the repo to pnpm:
  - Removed stale `package-lock.json`.
  - Added `packageManager: pnpm@11.5.0`.
  - Added `engines.node: 24.x` and `engines.pnpm: 11.x`.
  - Added `.nvmrc` with Node 24.
  - Moved pnpm build-script policy to `pnpm-workspace.yaml`.
- Updated dependencies:
  - Next.js 15.2.4 → 16.2.6.
  - React / React DOM → 19.2.6.
  - Thirdweb → 5.120.0.
  - Viem pinned at 2.51.3 to satisfy newer Web3 peer expectations.
  - Vercel Analytics → 2.0.1.
  - Tailwind CSS / PostCSS integration → 4.3.0.
  - Type packages updated to React 19 and Node 24.
  - TypeScript updated to 5.9.3.
- Replaced `next lint` with `eslint .`.
- Migrated ESLint config to Next 16 flat config and ignored generated files.
- Refactored `TokenClaim` derived state so React Hooks lint no longer reports synchronous state updates inside effects.
- Added `.env.example` with `NEXT_PUBLIC_TW_CLIENT_ID` and allowed it through `.gitignore`.
- Updated `src/config/client.ts` so the app can build without local secrets, using an Evergreen placeholder client ID when the real public Thirdweb client ID is absent.
- Replaced the default Create Next App README with project-specific Evergreen documentation.

## Build Script Policy

- `sharp` build script is allowed because the previous repo configuration already allowed `sharp`.
- Native optional extras `bufferutil`, `keccak`, and `utf-8-validate` are explicitly denied to keep the build-script surface smaller.

## Deferred Items

- `@types/node@25` is deferred because the repo targets Node 24.x.
- `typescript@6` is deferred because Solana/Thirdweb transitive packages currently peer to TypeScript 5.x.
- `eslint@10` was tested and deferred because lint fails with `scopeManager.addGlobals is not a function` in the current Next/TypeScript ESLint stack.
- Remaining audit findings after `pnpm audit --fix update`:
  - `postcss` via Next.js internals.
  - `ws` via Web3 wallet dependencies.
  - `uuid` via MetaMask/WalletConnect dependencies.

## Validation

- `corepack pnpm install` completed successfully.
- `corepack pnpm lint` passed.
- `corepack pnpm build` passed on Next.js 16.2.6 with Turbopack.
- `corepack pnpm outdated` only reports intentionally deferred packages.
- `corepack pnpm audit --audit-level moderate` reports 3 remaining moderate transitive findings.
- `corepack pnpm peers check` reports remaining transitive peer warnings for Solana sysvars and `use-sync-external-store` React peer range.
- Live URL check: `https://endhonesa.com/` returned Vercel Challenge `HTTP 429`, reachable but blocked.
